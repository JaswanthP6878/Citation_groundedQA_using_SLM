name: rag_env
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip

  - numpy
  - pytest
  - tqdm

  - pytorch

  - pip:
      - transformers
      - accelerate
      - sentence-transformers
      - datasets
      - faiss-cpu
      - rank-bm25
